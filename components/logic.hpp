#ifndef LOGIC_H_
#define LOGIC_H_

struct _player{};

struct collision{
    int x, y;
    int w, h;
};


struct window{
    int id;
    int x, y;
    int w, h;
};

struct transitioning{
    float factor;
};

struct _isactive{};


#endif // LOGIC_H_
