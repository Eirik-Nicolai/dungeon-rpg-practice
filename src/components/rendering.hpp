#ifndef RENDERING_H_
#define RENDERING_H_
#include <string>

struct _renderable{};

struct simple_appearence{
    std::string c;
};

struct combat_appearence{
    std::string c;
};

#endif // RENDERING_H_
